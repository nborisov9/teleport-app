import React from 'react';

import { Header } from './components/Header';

import './app.scss';

const App: React.FC = (): React.ReactElement => {
  return (
    <div>
      <Header />
    </div>
  );
};

export default App;
